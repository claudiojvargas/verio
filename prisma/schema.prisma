generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Product entities will be introduced by vertical slices after the Phase 1 and
// Phase 2 validation gates pass. Keeping the initial schema empty prevents the
// architecture document from becoming an unvalidated database implementation.
